const CONFIG = {
    // During local development, this points to localhost:8080.
    // Replace this with your Render web service URL (e.g. 'https://lead-qualifier-backend.onrender.com') when deploying globally.
    API_BASE_URL: 'http://localhost:8080'
};
